import { DateCommonDirective } from './date-common.directive';

describe('DateCommonDirective', () => {
	it('should create an instance', () => {
		const directive = new DateCommonDirective();
		expect(directive).toBeTruthy();
	});
});
