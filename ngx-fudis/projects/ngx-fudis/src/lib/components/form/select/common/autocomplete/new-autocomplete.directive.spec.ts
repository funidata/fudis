import { NewAutocompleteDirective } from './new-autocomplete.directive';

describe('NewAutocompleteDirective', () => {
  it('should create an instance', () => {
    const directive = new NewAutocompleteDirective();
    expect(directive).toBeTruthy();
  });
});
