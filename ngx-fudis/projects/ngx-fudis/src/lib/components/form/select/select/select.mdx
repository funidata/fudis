import { Meta, Canvas, ArgTypes } from "@storybook/blocks";
import { SelectComponent } from "./select.component";
import * as Stories from "./select.stories";
import { selectArgsTableExclude } from "../../../../utilities/storybook";

<Meta title="Components/Form/Select/Select" />

# Select (Single Select)

Select Component is a form component displaying a dropdown list of selectable options to choose a single value.

## Properties

### Common Properties and Features

Select Component shares multiple common features with Multiselect Component e. g. text labels, autocompletion and grouping of options.

Therefore please check also [Common Features](/docs/components-form-select-common-features--documentation) documentation.

### Form Control

Select Component requires Angular Form Control. Type and value of control can be either `null` or `FudisSelectOption`.

```
// TypeScript

defaultOption: FudisSelectOption = {value: 'default-option-id', label: 'Default Option'}

myControl: new FormControl<null | FudisSelectOption>(defaultOption)

// HTML Template

<fudis-select [control]="myControl">
  ...
</fudis-select>

```

## Example

<Canvas of={Stories.Example} />

## Properties

<ArgTypes of={SelectComponent} exclude={selectArgsTableExclude} />
