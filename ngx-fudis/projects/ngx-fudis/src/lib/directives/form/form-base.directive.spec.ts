import { FormBaseDirective } from './form-base.directive';

describe('FormBaseDirective', () => {
	it('should create an instance', () => {
		const directive = new FormBaseDirective();
		expect(directive).toBeTruthy();
	});
});
