import { GridItemApiDirective } from './grid-item-api.directive';

describe('GridItemApiDirective', () => {
	it('should create an instance', () => {
		const directive = new GridItemApiDirective();
		expect(directive).toBeTruthy();
	});
});
