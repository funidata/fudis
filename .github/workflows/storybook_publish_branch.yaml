# WIP
# Should be merged with other Storybook workflows once ready.
name: Publish Storybook by branch

on:
  push:
    branches:
      - basse-testaa-2
  # FIXME: Use the trigger spec below once this action is verified to work.
  #  branches-ignore:
  #    - main

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Install dependencies
        run: npm ci
        working-directory: ngx-fudis
      - name: Build Storybook
        run: npm run build-storybook -- -o target/basse-testaa-2
        working-directory: ngx-fudis
      - name: Upload
        uses: actions/upload-pages-artifact@v1.0.7
        with:
          path: ngx-fudis/target

  deploy:
    needs: build
    runs-on: ubuntu-22.04
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v1.2.4
